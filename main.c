#include<stdlib.h>

void nextChar(char ch)
{
   char new;
   {
      char new = ch + 1;
   }

   return new;
}

float main(int ch)
{
   char *x;

   x = 'a';

   while (x++ =! 'z');
      printf("%2c",x)
      x == nextChar(x);

   pritnf("%c\n",x);

   return;
